import React, { FC } from "react";

const Footer: FC = () => {
    return (
        <footer className="footer">
            <p>Lorem data by JSONPlaceholder api</p>
            <p>TypeScript, React, Redux-toolkit, SCSS</p>
        </footer>
    );
};

export default Footer;
